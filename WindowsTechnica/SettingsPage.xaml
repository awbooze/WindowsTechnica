<Page
    x:Class="WindowsTechnica.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WindowsTechnica"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <RelativePanel>
		<CommandBar x:Name="commandBar"  VerticalAlignment="Bottom" HorizontalContentAlignment="Center" IsOpen="False"
                    RelativePanel.AlignBottomWithPanel="True" RelativePanel.AlignRightWithPanel="True" RelativePanel.AlignLeftWithPanel="True">
			<CommandBar.Content>
				<TextBlock x:Name="titleBar" Text="Settings" Height="auto" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" TextWrapping="Wrap" FontSize="16" Margin="10,10,10,10"/>
			</CommandBar.Content>
		</CommandBar>
	</RelativePanel>
</Page>
